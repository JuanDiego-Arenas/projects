// function crearPersona(name, lastName) {
// 	return { name, lastName };
// }
const crearPersona = (name, lastName) => ({ name, lastName });

const person = crearPersona('Juan Diego', 'Arenas');
console.log(person);
console.log(person);
